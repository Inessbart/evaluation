{% extends 'base.html.twig' %}

{% block title %}Hello AccueilController!{% endblock %}

{% block body %}

<div class="container">
<div id="carouselExampleControls" class="carousel slide" data-bs-ride="carousel">
  <div class="carousel-inner">
    <div class="carousel-item active">
      <img src="images/Capture.JPG" class="d-block" alt="..." class="container-fluid" style="width: 1000px; height:auto">
    </div>
    <div class="carousel-item">
      <img src="images/Capture01.JPG" class="d-block" alt="..." class="container-fluid" style="width: 1000px; height:auto">
    </div>

  </div>
  <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Previous</span>
  </button>
  <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Next</span>
  </button>
</div>

<br><br>

<h1 class="text-center">NOUVEAUTÉS</h1>

<br><br>

<div class="row">
  {% for produit in produits|slice(0, 4) %}
  
  <div class="col-md-3">
      <div class="card m-2" style="width: 17rem;">
          <h5 class="card-title">{{ produit.marque }}</h5>
          <h6 class="card-title">{{ produit.designation }}</h6>
          <img  src="images/{{ produit.image }}" class="card-img-top" alt="..." style="width: 70px; height:auto">
          <div class="card-body">
            <h6 class="card-title">{{ produit.prix}}</h6>
            <h6 class="card-title">{{ produit.disponible ? 'Yes' : 'No' }}</h6>
  
            <a href="produits" class="btn btn-secondary">En savoir plus</a>
            <a href="panier" class="btn btn-primary">Ajouter au panier</a>
          </div>
        </div>
  </div>
  
  <br><br>
  
  {% endfor %}
  </div>
{% endblock %} 
